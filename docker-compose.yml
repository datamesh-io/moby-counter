web:
  build: .
  ports:
    - "8100:80"
  links:
    - "postgres:postgres"
postgres:
  image: postgres
  #volume_driver: dm
  #volumes:
  # - "moby_counter:/var/lib/postgresql/data"
  environment:
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=postgres
    - POSTGRES_DB=moby
